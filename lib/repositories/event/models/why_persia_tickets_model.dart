import 'package:billit_now/apps/helper/utils/app_icon_name.dart';

import '../../base/base_model.dart';

class WhyPersiaTicketsModel implements BaseModel {
  final String icon;
  final String title;
  final String desc;

  WhyPersiaTicketsModel({
    required this.icon,
    required this.title,
    required this.desc,
  });
}
