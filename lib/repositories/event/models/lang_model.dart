import '../../base/base_model.dart';

class LangCurrencyModel implements BaseModel {
  final String title;
  final String subTitle;

  LangCurrencyModel({
    required this.title,
    required this.subTitle,
  });
}
